 databaseChangeLog:
   - changeSet:
       id: "maintenance_mode_settings"
       author: mbemis
       changes:
          - createTable:
               tableName: maintenance_mode
               columns:
                - column:
                     { name: message, type: text, constraints: { nullable: true } }
                - column:
                     { name: password, type: text, constraints: { nullable: true } }
                - column:
                     { name: enabled, type: boolean, constraints: { nullable: false } }
                - column:
                     { name: disable_scheduled_jobs, type: boolean, constraints: { nullable: false } }
